# SSH Interface v2 has moved

This module now lives in the **Crestron Modules** catalog:

**https://github.com/Prophet6/Crestron-Modules/tree/main/ssh-interface-v2**

The PC SSH/TCP mock is still a separate repo: [ssh-tcp-mock](https://github.com/Prophet6/ssh-tcp-mock).
